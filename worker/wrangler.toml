name = "prunesh-marketplace-worker"
main = "src/index.js"
compatibility_date = "2025-01-01"
workers_dev = true
preview_urls = false

# MARKETPLACE_TOKEN must be set as a secret:
#   wrangler secret put MARKETPLACE_TOKEN
#
# It should be a GitHub fine-grained PAT with:
#   Repository access: prunesh/marketplace only
#   Permissions: Actions (write)
